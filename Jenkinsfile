pipeline {
    agent any
    stages {
        stage('Image Build') {
            steps {
                sh """
                    echo "${ACCOUNT_ID}"
                """
            }
        }
    }
}